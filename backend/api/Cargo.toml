[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.37.0", features = ["full"] }
anyhow = "1.0.82"

tracing = "0.1.40"
tracing-subscriber = "0.3.18"

axum = { version = "0.7.5", features = ["tracing"] }

